name: snorkel_track
description: SnorkelTrack MVP - GPS spots & nav.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0  # GPS core
  permission_handler: ^11.0.1  # Perms
  flutter_compass: ^0.8.0  # For arrow heading (add for compass)
  shared_preferences: ^2.2.2  # Offline storage (MVP; swap to Hive later)
  provider: ^6.1.1  # State management

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Add assets later: images/ for water icons
